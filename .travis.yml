language: node_js
node_js:
  - '4.2.0'
cache:
  directories:
    - node_modules
before_script:
  - npm install knex -g
  - psql -c 'create database foodujour_test;' -U postgres
  - NODE_ENV=test knex migrate:latest
